version: '3.8'

services:
  gam-ai:
    build: .
    container_name: gam-ai
    ports:
      - "8080:8080"
    volumes:
      - gam-ai-data:/app/data
    restart: unless-stopped

volumes:
  gam-ai-data:
